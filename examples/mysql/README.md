# Aurora MySQL example

Creates a private, encrypted Aurora MySQL I/O-Optimized cluster with one writer, one reader, backtracking, Enhanced Monitoring, log exports, and an RDS-managed master password. Supply a supported engine version and existing network inputs.

Supply a unique final snapshot identifier; change it before replacing or recreating a cluster if that snapshot name already exists.
