# Aurora PostgreSQL example

Creates a private, encrypted Aurora PostgreSQL cluster with one writer, one reader, Enhanced Monitoring, PostgreSQL log exports, and an RDS-managed master password. Supply an engine version supported in the selected Region plus existing VPC and private-subnet inputs.

Supply a unique final snapshot identifier; change it before replacing or recreating a cluster if that snapshot name already exists.
